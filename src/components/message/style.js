import styled from '@emotion/styled'
import { Input as InputStyle } from '@mui/material'
import Send from '@mui/icons-material/Send';

export const Input = styled(InputStyle)`
color: white;
padding: 10px 15px;
font-size: 15px
`

export const SendIcon = styled(Send)`
cursor: pointer;
color: #2b5278;
`